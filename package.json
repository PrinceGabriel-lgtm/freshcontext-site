{
  "name": "freshcontext-site",
  "private": true,
  "scripts": {
    "check": "npm run check:commercial && npm run check:html && npm run check:a11y",
    "check:html": "html-validate *.html",
    "check:a11y": "playwright test",
    "check:commercial": "node --test ops/commercial/generate-pack.test.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.60.0",
    "html-validate": "^11.4.0",
    "wrangler": "^4.99.0"
  }
}
