.wrangler/
.dev.vars
.env
.env.*
node_modules/
playwright-report/
test-results/

# Written by the deploy job immediately before `wrangler deploy`, so the live
# site can be asked which commit it is serving. Generated, never committed: a
# checked-in copy would be stale the moment anything else merged.
build.json
.DS_Store
Thumbs.db

# Generated client commercial packs — may contain confidential client data.
commercial-packs/
